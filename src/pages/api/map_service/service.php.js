// API endpoint для виджета СДЭК.
// Важно: оставляем ".php" в пути, потому что часть прокси/инфраструктуры
// может пропускать только такие URL или ожидать legacy-формат.
import handler from "./service";

export default handler;


